<Page
    x:Class="IIUWr.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IIUWr"
    xmlns:model="using:IIUWr.Fereol.Model"
    xmlns:conv="using:LionCub.UI.Xaml.Data.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <CollectionViewSource x:Key="CoursesPerSemesterCollection"
                              IsSourceGrouped="True"
                              Source="{x:Bind VM.CoursesService.Semesters, Mode=OneWay}"
                              ItemsPath="Courses" />
        <local:ObjectConverter x:Key="ObjectConverter" />
        <conv:LocaleTextConverter x:Key="CourseTypeNameConverter" Map="CourseType" />
        <conv:LocaleTextConverter x:Key="LocaleTextConverter" />
        <conv:BooleanConverter x:Key="BooleanConverter" />
    </Page.Resources>

    <Grid x:Name="root" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="900" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="semesters.(Grid.RowSpan)" Value="2" />
                        <Setter Target="root.RowDefinitions[1].Height" Value="2*" />
                        <Setter Target="root.ColumnDefinitions[1].Width" Value="2*" />
                        <Setter Target="root.ColumnDefinitions[2].Width" Value="0" />
                        <Setter Target="description.(Grid.Column)" Value="1" />
                        <Setter Target="description.(Grid.Row)" Value="1" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="semesters.(Grid.RowSpan)" Value="2" />
                        <Setter Target="root.RowDefinitions[1].Height" Value="2*" />
                        <Setter Target="root.ColumnDefinitions[2].Width" Value="0" />
                        <Setter Target="description.(Grid.Column)" Value="1" />
                        <Setter Target="description.(Grid.Row)" Value="1" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="semesters" ItemsSource="{Binding Source={StaticResource CoursesPerSemesterCollection}}" SelectedItem="{x:Bind VM.SelectedCourse, Mode=TwoWay, Converter={StaticResource ObjectConverter}}">
            <ListView.Header>
                <Button x:Uid="RefreshButton" Click="{x:Bind VM.RefreshSemesters}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="32" />
            </ListView.Header>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="model:Semester">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Bind Year}" Margin="0,0,7,0" />
                                <TextBlock Text="{x:Bind YearHalf, Converter={StaticResource LocaleTextConverter}}" />
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
        <StackPanel x:Name="courseInfo" Grid.Column="1" Margin="10" Visibility="{x:Bind VM.SelectedCourse, Mode=OneWay, Converter={StaticResource BooleanConverter}}">
            <Button x:Uid="RefreshButton" Click="{x:Bind RefreshDescription}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="32"/>
            <TextBlock TextWrapping="Wrap" Text="{x:Bind VM.SelectedCourse.Name, Mode=OneWay}" />
            <TextBlock TextWrapping="Wrap" Text="{x:Bind VM.SelectedCourse.Type.NameKey, Mode=OneWay, Converter={StaticResource CourseTypeNameConverter}}" />
            <StackPanel Orientation="Horizontal" Visibility="{x:Bind VM.SelectedCourse.ECTS, Mode=OneWay, Converter={StaticResource BooleanConverter}}">
                <TextBlock x:Uid="/Course/ECTSLabel" Margin="0,0,10,0" />
                <TextBlock Text="{x:Bind VM.SelectedCourse.ECTS, Mode=OneWay}" />
            </StackPanel>
        </StackPanel>
        <WebView x:Name="description" Grid.Column="2" />

    </Grid>
</Page>
