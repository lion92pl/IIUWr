namespace UnitTests
{
    internal static class Categories
    {
        // Courses failing also on web page (no filter pane, all semesters visible, etc)
        public const string FailingCourses = nameof(FailingCourses);

        public const string PassingCourses = nameof(PassingCourses);

        public const string Parsing = nameof(Parsing);
    }
}
